import { DataSortingPipe } from './data-sorting.pipe';

describe('DataSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new DataSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
